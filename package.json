{
  "name": "monochain",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for LangChain/LangGraph examples and projects",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "format": "pnpm -r format",
    "format:check": "pnpm -r format:check"
  },
  "author": "Viacheslav Ptsarev",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.7.1",
    "typescript": "~5.9.3"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
